<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Coordinates and Wi-Fi display -->
    <TextView
        android:id="@+id/coordinatesText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Coordinates will appear here"
        android:textSize="16sp"
        android:padding="8dp" />

    <TextView
        android:id="@+id/wifiText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Wi-Fi info will appear here"
        android:textSize="16sp"
        android:padding="8dp" />

    <!-- Get Location button -->
    <Button
        android:id="@+id/getLocationBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get Location" />

    <!-- View All Records button -->
    <Button
        android:id="@+id/viewAllBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="View All Records"
        android:layout_marginTop="8dp" />

    <!-- Clean Records button -->
    <Button
        android:id="@+id/cleanRecordsBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Clean Records"
        android:layout_marginTop="8dp" />


    <!-- TextView to display all database entries -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp">

        <TextView
            android:id="@+id/allRecordsText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Database entries will appear here"
            android:textSize="14sp"
            android:padding="8dp" />
    </ScrollView>

</LinearLayout>
